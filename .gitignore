/target/

# Cargo.lock IS tracked: this fork ships a binary (the serve/generate_audio
# deployment on halo), and the nixcfg package needs the exact dependency
# graph inside the fetched source to build reproducibly.

# Downloaded test data (not tracked in git)
/test_data/

# Python virtual environments
.venv/
tools/.venv/

# Generated audio files
*.wav

# Reference audio for voice cloning example (Qwen3-TTS official test audio)
!/examples/data/clone_2.wav

# README sample assets
!/assets/audio/*.wav
!/assets/images/*.png

# Python cache
__pycache__/
*.pyc

# UV lock file (large, can be regenerated)
uv.lock
/models/
bench-*.json
trace.json
perf.data
perf.data.old
nsys_report.*
/flamegraph.svg
!/docs/flamegraph.svg
.direnv/
